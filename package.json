{
  "name": "hsncementandsteel",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "codegen": "convex codegen",
    "test": "node --test",
    "build": "mkdir -p dist && cp -r assets index.html favicon.svg icons.svg *.png *.jpg *.svg dist/ && cp index.html dist/404.html"
  },
  "dependencies": {
    "convex": "^1.22.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vite": "^6.0.0"
  }
}
